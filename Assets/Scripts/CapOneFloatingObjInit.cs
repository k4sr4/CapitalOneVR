using UnityEngine;
using System.Collections;

public class CapOneFloatingObjInit : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.transform.localScale = new Vector3 (2, 5, 5);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
